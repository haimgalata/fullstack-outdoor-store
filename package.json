{
  "name": "fullstack-outdoor-store",
  "version": "1.0.0",
  "private": true,
  "description": "Outdoor equipment store — React client and Express API (see README)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "npm start --prefix server"
  }
}
